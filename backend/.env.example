# Gemini API key — get one at https://aistudio.google.com/app/apikey
# Used for live MFN tariff lookup via gemini-1.5-flash.
# If left empty the system falls back to the hardcoded tariff table.
GEMINI_API_KEY=

# Full path to your DOSBox executable.
# Windows example: C:\Program Files (x86)\DOSBox-0.74-3\DOSBox.exe
# Linux example:   /usr/bin/dosbox
# If left empty the system uses "dosbox" (must be on PATH).
DOSBOX_PATH=dosbox.exe
